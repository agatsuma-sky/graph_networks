# @package _global_

# to execute this experiment run:
# python train.py experiment=CauSTG

defaults:
  - override /data: electricity
  - override /model: electricity
  - override /callbacks: default
  - override /trainer: gpu

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters

tags: ["electricity", "CauSTG"]

seed: 99

# trainer:
#   min_epochs: 10
#   max_epochs: 10
# #  gradient_clip_val: 0.5

# model:
#   optimizer:
#     lr: 0.001
#   net:
#     lin1_size: 128
#     lin2_size: 256
#     lin3_size: 64
#   compile: false

# data:
#   batch_size: 64

logger:
  wandb:
    tags: ${tags}
    group: "electricity"
  aim:
    experiment: "electricity"
